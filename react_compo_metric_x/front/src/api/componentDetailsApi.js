import { instance } from '../helpers';

// export const fetchComponentGlobalStats = async (name) => {
//     try {
//         const response = await instance.get(`/component/${name}/global-info`);
//         return response.data
//     } catch (error) {
//         console.error(`Erreur lors de la récupération des données du composant ${name}: ${error.message}`);
//         throw error.message;
//     }
// };

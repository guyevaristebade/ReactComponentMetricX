import { instance } from '../helpers';

/*export const getComponentsName = async() =>{
    try {
        const result = await instance.get("/component/componentName")
        return result.data
    }catch (e) {
        return e.message
    }
}*/
